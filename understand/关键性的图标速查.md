以下是论文中（除去交易模拟部分）所有核心图表的完整清单及代码生成能力评估：

| 编号        | 名称             | 作用                                                         | 代码能否生成                                                 |
| ----------- | ---------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **Table 1** | ADF 检验结果     | 展示原始序列与 VMD 分解后 IMF 的平稳性对比                   | ⚠️ 部分可生成 — `ADF.py` 存在但硬编码了路径且只针对单序列，未集成到管线，需手动适配 |
| **Table 4** | 最优 VMD 参数    | 每支股票的 K 和 α 值                                         | ✅ **可生成** — `GA_VMD.py` 产出 `_GA.csv`，包含每支股票的 (K, α) 数据 |
| **Table 5** | 全模型性能对比   | 本方法 vs 13 种基线（ARIMA, GRU, ..., VGC-GAN）在 5 个指标上的表现 | ❌ **不可生成** — 代码只运行 REGCN 模型，不包含任何基线方法实现 |
| **Table 6** | 消融实验         | 9 种变体（替换分解算法、替换单图、去掉 GCN、去掉正则化等）的对比 | ❌ **不可生成** — 代码不含消融模式的开关，需要手动修改后多次运行 |
| **Table 7** | 滑动窗口敏感性   | seq_len ∈ {7, 15, 30, 45, 60} 的性能变化                     | ❌ **不可生成** — 需要手动修改 `config.ini` 后多次运行，无自动化脚本 |
| **Fig. 3**  | 预测效果对比图   | 本方法与部分基线的预测曲线可视化对比                         | ⚠️ **部分可生成** — `REGCN.py` 会输出单条预测 vs 真实曲线图（`p红/蓝线对比.png`），但只针对本方法，无基线对比 |
| **Fig. 4**  | 正则化系数敏感性 | 不同 λ₁ (r_mse) 和 λ₂ (r_acc) 组合对性能的影响（热力图）     | ❌ 需要遍历超参数组合多次运行后汇总，无自动化脚本             |

### 核心结论

**能稳定产出的：** Table 4（VMD 参数）、Table 5 中 REGCN 自身那一行、Fig. 3 中的本方法预测曲线。

**无法产出的根本原因不是代码缺失，而是缺少「自动化实验框架」**：基线对比、消融实验、超参数扫描都需要手动修改配置 → 运行 → 汇总结果，当前代码没有做实验编排的脚本。如果要补上，最直接的方式是给 `main.py` 加一个 `--eval` 模式来自动遍历这些配置组合。